P1 remediation for foi-process #96.

Scope:
- Exact registered-path and template-contract strict validation.
- Exact path+SHA-256 legacy whitelist.
- RED-first adversarial tests for strict marker removal, successor registration/rename, and whitelist hash tampering.

Validation:
- Focused semantic suite passed with 13 adversarial mutations.
- Full scripts/ci-local.sh passed using an isolated Python validation environment.
- Ruff lint/format and diff integrity passed.

Environment:
- Repository Co-Researcher bootstrap executable was unavailable.
- Temporary validation dependencies did not change repository manifests or lockfiles.

Restrictions observed:
- No push, pull request, merge, retrieval, publication, or legal conclusion.
