# Review and promote FOI process contracts

Review the v3 `ProcessEvent`, object/link/change, `DocumentSignal`, epistemic status, finding layers, and privacy concepts against FOI-O. Promote only domain-semantic contracts and vocabulary terms; leave replay/storage mechanics in `foi-process` and capture fields in `fyi-core`.

Acceptance: FOI-O term mapping, JSON Schema/SHACL profile, human-certification boundary tests, compatibility fixtures, and a generated Rust consumer surface or schema crate.
