# Integration tests for existing AppendableOCEL

Rust4PM 0.6 already provides `AppendableOCEL`. Add generic tests/documentation for declaration order, duplicate object/event IDs, repeated declarations, forward references, finalize behaviour, and large incremental appends. Use FOI-shaped synthetic data only as a benchmark fixture; keep the API domain-neutral.
