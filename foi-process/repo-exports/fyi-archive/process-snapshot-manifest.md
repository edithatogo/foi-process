# Publish derived process snapshot manifests

Keep `fyi-archive` storage-first. Add optional links/manifests for separately generated process artefacts: source revision, input manifest hash, normaliser/profile versions, Rust4PM/foi-process versions, privacy profile, output checksums, row counts, SBOM, and release/DOI references. Derived artefacts must never replace immutable capture evidence.
