# Background adoption study: rules/process coupling

Keep `rulesandprocesses` off the runtime path. Record how ProcessEvent, EvidenceDelta, DocumentSignal, and ConformanceTrace map to PIC semantics/crosswalk/fixture/trace ideas; measure cross-engine divergence; promote only contracts with named consumers and working implementations.
