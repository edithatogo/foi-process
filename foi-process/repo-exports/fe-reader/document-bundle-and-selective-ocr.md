# DocumentBundle and selective OCR pipeline

Make `fe-reader` the owner of reproducible document evidence: born-digital extraction, quality assessment, selective page/region OCR, page rendering, reading order/layout/table geometry, hashes, model/runtime/version/license, confidence, and warnings. Emit the v3 `DocumentBundle`; store large text as blobs referenced by stable IDs.
